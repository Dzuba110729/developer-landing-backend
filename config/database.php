<?php

return [

    /*
    |--------------------------------------------------------------------------
    | База данных не используется
    |--------------------------------------------------------------------------
    |
    | Проект хранит все данные (логи обращений, статистику, rate-limit) в
    | файловой системе — см. App\Repositories\ContactRepository, а также
    | CACHE_STORE=file и SESSION_DRIVER=file в .env. Подключение БД оставлено
    | пустым, чтобы не тянуть лишние зависимости и не усложнять деплой.
    |
    */

    'default' => env('DB_CONNECTION', ''),

    'connections' => [],

    'migrations' => [
        'table' => 'migrations',
        'update_date_on_publish' => true,
    ],

];
